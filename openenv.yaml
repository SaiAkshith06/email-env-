name: adaptive_triage_env
version: 0.2.0
description: "RL environment for adaptive triage – classify support emails by category, priority and ambiguity"
author: "Kola"

tags:
  - openenv
  - adaptive-triage
  - nlp
  - customer-support

models:
  action: EmailAction
  observation: EmailObservation
  state: EmailState
  reward: float

environment:
  max_steps: 60
  deterministic: true
