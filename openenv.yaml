name: email_env_v2
version: 0.2.0
description: "RL environment for email triage – classify support emails by category, priority and ambiguity"
author: "Kola"

tags:
  - openenv
  - email-triage
  - nlp
  - customer-support

models:
  action: EmailAction
  observation: EmailObservation
  state: EmailState
  reward: float

environment:
  max_steps: 60
  deterministic: true
