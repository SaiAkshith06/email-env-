name: email_env_v2
version: 0.2.0

tags:
  - openenv
  - email-triage
  - nlp
  - customer-support

models:
  action: EmailAction
  observation: EmailObservation
  state: EmailState
  reward: float

environment:
  max_steps: 60
  deterministic: true
